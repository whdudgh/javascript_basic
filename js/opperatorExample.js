//연산자 연습

// == | ===의 차이
let strNum = '100';
let num = 100;
//동등연산자(값만 비교)
console.log(strNum == num);
//일치연산자(타입까지 비교)
console.log(strNum === num);

console.log(strNum !== num);

console.log(undefined == null);
console.log(undefined === null);